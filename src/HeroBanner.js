import React from "react";
import { Box, Image } from "@chakra-ui/react";
const HeroBanner = ({ animeList }) => {
  return (
    <Box>
      {/* <Image
        src={animeList[1].attributes.coverImage.large}
        alt={animeList[1].attributes.titles.en}
      /> */}
    </Box>
  );
};

export default HeroBanner;
